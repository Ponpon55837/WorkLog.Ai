WORK_INTELLIGENCE_PORT=3210
WORK_INTELLIGENCE_DB=./data/work-intelligence.sqlite
# Comma-separated browser origins allowed to call the REST API directly.
# Keep this limited to the local Web UI; do not use *.
WORK_INTELLIGENCE_ALLOWED_ORIGINS=http://127.0.0.1:5966,http://localhost:5966
